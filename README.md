# AmityWebsite

Semi-Working Site as a proof of concept, will eventually rewrite it at some point to make the code cleaner and allow for it to be responsive for mobile rather than the P.O.S that it looks like rn for mobile or different sized screens.
